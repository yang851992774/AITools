现在需要增加一个安装xapk的功能
  - xapk是一种特殊格式的apk文件,它是一个zip文件，里面包含了多个apk文件，每个apk文件都是一个单独的app
  - 文件夹会包含一个manifest.json文件，里面包含了app的详细信息，比如app的名称，图标，版本号等等
  - 需要向用户提供一个下载地址，这个地址是xapk文件的下载地址
  - 下载完成后解压为提取 "package_name".apk的文件进行安装
  - 下载的xapk无需清理，但是其他文件需要清理。

